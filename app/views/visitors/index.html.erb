<div class="row d-flex justify-content-end">
  <%= link_to "Play", plays_path%>
</div>
<h3>Welcome</h3>
<%= form_for @photo do |f| %>
  <p>
    <%= f.label "Add some file"%><br/>
    <%= f.file_field :content, multiple: true %>
  </p>
  <p>
    <%= f.submit "Save" %>
  </p>
<% end %>
